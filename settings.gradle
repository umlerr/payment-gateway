pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'payment-gateway'

include 'common', 'service-payment', 'service-notification', 'service-analytics'
